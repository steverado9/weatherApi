import { Router } from "express";

class CityRoutes {
    router = Router();

    constructor() {}

    initializeRoutes() {
        this.router.get(`xyz`,)
    }
}

export default new CityRoutes().router;
